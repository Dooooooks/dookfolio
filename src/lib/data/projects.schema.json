{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "ProjectsSchema",
	"type": "array",
	"items": {
		"type": "object",
		"required": ["title", "description", "tags"],
		"properties": {
			"title": {
				"type": "string",
				"description": "Project display title"
			},
			"description": {
				"type": "string",
				"description": "Short project description or summary"
			},
			"tags": {
				"type": "array",
				"items": { "type": "string" },
				"description": "Technologies and categories (e.g. React, Vite, Godot)"
			},
			"demo_url": {
				"type": ["string", "null"],
				"description": "Live demo or download link, or null"
			},
			"github_url": {
				"type": ["string", "null"],
				"description": "Public source code URL on GitHub, or null"
			},
			"cover_url": {
				"type": ["string", "null"],
				"description": "Relative cover image path (e.g. /ProjectPictures/MyProject.png) or null"
			},
			"date": {
				"type": "string",
				"pattern": "^\\d{4}-\\d{2}$",
				"description": "Date in YYYY-MM format (e.g. 2026-09) used for sorting"
			}
		},
		"additionalProperties": false
	}
}
