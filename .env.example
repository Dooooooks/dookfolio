# Optional: GitHub Personal Access Token (classic or fine-grained) with read:user scope.
# Enables direct queries to GitHub's official GraphQL API and displays private contributions.
# If not provided, public contribution scraping is used as fallback.
GITHUB_TOKEN=
